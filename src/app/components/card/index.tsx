import { ITicketData } from "@/app/api/models/Ticket";
import React from "react";

type Props = {
  ticket: ITicketData;
};

const Card = ({ ticket }: Props) => {
  return <div>Card</div>;
};

export default Card;
